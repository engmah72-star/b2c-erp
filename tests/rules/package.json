{
  "name": "b2c-rules-tests",
  "version": "1.0.0",
  "description": "Firestore rules regression tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node role-escalation.test.js && node financial-write.test.js && node client-support-delivery.test.js && node action-items.test.js",
    "test:role": "node role-escalation.test.js",
    "test:financial": "node financial-write.test.js",
    "test:messaging": "node client-support-delivery.test.js",
    "test:storage": "node storage-order-upload.test.js",
    "test:ci": "node role-escalation.test.js && node financial-write.test.js && node client-support-delivery.test.js && node action-items.test.js && node storage-order-upload.test.js",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-test \"npm test\"",
    "test:emulator:storage": "firebase emulators:exec --only storage --project demo-test \"npm run test:storage\"",
    "test:emulator:ci": "firebase emulators:exec --only firestore,storage --project demo-test \"npm run test:ci\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase-tools": "^13.0.0"
  }
}
